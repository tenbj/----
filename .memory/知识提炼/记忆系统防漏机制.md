<!-- memory-version: 1.1.0 -->
# 知识提炼 · 记忆系统防漏机制

> 从研究过程中提炼的核心结论与知识点，持续更新。

---

## 2026-05-04 · 记忆记录遗漏的根因与防护

**核心观点**：
- 记忆系统有三层输出（对话记录、系统记录、知识提炼），缺一不可。但实际执行中，对话记录和知识提炼最容易被跳过。
- 根因不在于"规则不够"，而在于"规则被执行者当成了可选项"——记忆记录被当作"任务完成后的额外步骤"，而非回复本身的一部分。
- 防护不能只靠纪律，必须从规则文本和执行检查清单两个层面同时加固。

**关键框架/模型**：

### 三道防线

| 防线 | 位置 | 机制 |
|------|------|------|
| 第一道 | `memory-rules.md` 顶部 | 强制条款：「没有记录 = 回复没完」 |
| 第二道 | `记忆管理 Skill` 检查清单第 0 项 | 自检：「若正阅读此清单但记忆未写入→立即写入再发回复」 |
| 第三道 | 同上，第 6 项 | 数量自检：「对话记录文件数 ≥ 子项目文件夹数」 |

### 路由 BOTH 的陷阱

- 涉及 output/ 文件增删改 + 系统机制变更的项目，必须判 BOTH，不能只判 SYSTEM
- 不要以"这不只是补丁/延续"为由跳过 PROJECT 侧记录
- 判断依据：看在哪个子项目文件夹里做，而非"改的是什么"

### 知识提炼的触发缺口

- `memory-rules.md` 写「每次创建或修改 output/ 文件后必须追加知识提炼」
- `记忆管理 Skill` 写知识提炼是**「手动触发」**
- 二者矛盾给了跳过执行的台阶。已修复：统一为自动触发 + 三类触发条件。

## 2026-05-04 · 知识提炼自动触发规则

**核心观点**：
- 根因层面已分析清楚（Rule 和 Skill 自相矛盾），解决方案是统一触发规则，消除"手动触发"这个模糊词。
- 但也不能矫枉过正变成"每次改一个标点就要知识提炼"。

**关键框架/模型**：

### 三类自动触发条件

| 条件 | 触发场景 | 不触发 |
|------|---------|--------|
| A | output 文件有实质性内容变更（新建/MAJOR/MINOR） | PATCH（改错别字、调格式） |
| B | 系统治理变更产生可复用认知（发现架构问题、新设计模式） | 常规修改 |
| C | 用户明确要求 | — |

### 修改文件

| 文件 | 改动 |
|------|------|
| `记忆管理 Skill` 四类记忆表格 | "手动触发" → "每次 output/ 文件增删改后自动触发" |
| `记忆管理 Skill` 文件 C 写入规则 | 新增"触发条件（自动触发）"章节，三类条件 + 执行要求 |
| `memory-rules.md` | "必须追加知识提炼" → "根据触发条件判断，PATCH 不触发，MAJOR/MINOR 或系统治理认知必须触发" |

**与其他话题的关联**：
- → 项目运作机制：四层记忆的联动链路本身就容易在"对话记录"和"知识提炼"两个节点断链
- → 记忆系统治理方案：分流机制只管"写到哪"，不管"写了没"，缺执行保障层

---

## 版本更新记录

| 版本 | 日期 | 变更类型 | 变更描述 |
|------|------|--------|---------|
| v1.0.0 | 2026-05-04 | MAJOR | 首次沉淀记忆记录遗漏的根因分析与三道防线防护框架。 |
| v1.1.0 | 2026-05-04 | MINOR | 补充知识提炼自动触发规则：三类触发条件 + 修改文件对照。 |
