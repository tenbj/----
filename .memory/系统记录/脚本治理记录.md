# 系统记录 · 脚本治理

> 脚本 bug、编码问题、编译构建等脚本层治理记录，按时间追加。

---

## 2026-05-05 12:57

**用户问**：用 `项目规范化` 规范化 05 子项目，并注意代码路径需要配套调整。
**AI做了**：
1. 对 05 执行 PROJECT 备份并升级为 `05_工作区初始化工具_v1.7.2`。
2. 将 `src/`、`dist/`、`build_tmp/` 迁入 `03_代码程序_v0.0.0/`，补齐 `01_问题答疑_v0.0.0/`、`02_课题研究_v0.0.0/` 和 `目录.md`。
3. 核对 `build.ps1` 相对路径：脚本以自身 `src` 父目录定位 `dist` 与 `build_tmp`，迁移后构建逻辑无需改写。
4. 修正 `项目规范化` Skill 的必备 Skill 清单，加入 `课题研究`，并同步到 05 内置模板。
5. 将初始化工具版本推进到 v1.7.2 并重新编译 EXE；`normalize.ps1 --Check` 下 05 子项目为 PASS。

**产出文件**：`output/05_工作区初始化工具_v1.7.2/03_代码程序_v0.0.0/dist/初始化工作区_v1.7.2.exe`

---

## 2026-05-05 12:44

**用户问**：补上 v1.7.0 工作区初始化 EXE 漏掉的 `.temp` 根目录。
**AI做了**：
1. 复制出 `output/05_工作区初始化工具_v1.7.1/` 作为 PATCH 工作目录，并保留 v1.7.0 历史快照。
2. 在 `src/init_workspace.py` 的 `skeleton_dirs` 中新增 `.temp`，并同步头部说明、成功弹窗、旧项目升级确认文案。
3. 将 `SKELETON_VERSION` 和 `src/build.ps1` 构建版本更新为 v1.7.1，重新编译 EXE。
4. 用源码初始化临时工作区验证 `.temp` 创建成功，且 4 个 Rule、8 个 Skill 正常注入。

**产出文件**：`output/05_工作区初始化工具_v1.7.1/dist/初始化工作区_v1.7.1.exe`

---

## 2026-05-05 11:56

**用户问**：重新封装 05 子项目。
**AI做了**：
1. 为 05 子项目创建 v1.6.0 历史快照；因旧 live 目录一度被 Windows 拒绝重命名，改为复制出 `05_工作区初始化工具_v1.7.0` 后继续封装，并在完成后把旧 v1.6.0 live 目录移入 `.temp/封装旧版本_20260505115600/`。
2. 同步当前 `.agents/rules` 与 `.agents/skills` 到 `src/templates`，模板哈希校验通过；模板 Skill 集合包含 8 个 Skill，新增纳入 `课题研究`。
3. 更新 `SKELETON_VERSION` 与 `build.ps1` 版本号到 v1.7.0，并修正构建前清理逻辑，使 `dist` 只保留当前版本 EXE。
4. 重新编译 `dist/初始化工作区_v1.7.0.exe`，源码语法检查通过。

**产出文件**：`output/05_工作区初始化工具_v1.7.0/dist/初始化工作区_v1.7.0.exe`

---

## 2026-05-03 15:05

**用户问**：版本控制备份脚本和 Skill 的设计与实现
**AI做了**：创建了 `backup.ps1` 脚本和 `版本控制备份` Skill
**产出文件**：`.agent/skills/版本控制备份/scripts/backup.ps1`、`.agent/skills/版本控制备份/SKILL.md`

---

## 2026-05-03 15:28

**用户问**：子项目管理是否需要脚本，以及 Skill 设计
**AI做了**：创建了 `new_project.ps1` 脚本和 `子项目管理` Skill
**产出文件**：`.agent/skills/子项目管理/scripts/new_project.ps1`、`.agent/skills/子项目管理/SKILL.md`

---

## 2026-05-03 17:06

**用户问**：指出两个问题：`.memory` 结构应按类型分而非按项目分；Skill 备份应复制整个文件夹
**AI做了**：重构 `.memory` 目录结构，迁移文件到正确位置，修复 Skill 备份逻辑

---

## 2026-05-03 18:34

**用户问**：子项目管理中的命名规则需要调整为前方增加编号，并指出这一过程的记忆似乎没有触发
**AI做了**：更新 `子项目管理` Skill 与 `new_project.ps1` 脚本，将命名规则统一为 `编号_主题_时间戳`，并排查记忆路由，确认该类 Rule/Skill/脚本变更应写入系统治理记录，随后补写本条记录
**产出文件**：`.agent/skills/子项目管理/SKILL.md`、`.agent/skills/子项目管理/scripts/new_project.ps1`、`.memory/系统记录/rules-skills.md`

---

## 2026-05-03 18:56

**用户问**：指出 `.memory` 的备份逻辑仍有问题，并要求重新设计
**AI做了**：将 `.memory` 改为“当前态稳定文件名、历史态进入 `.history/.memory/`”的新模型，扩展 `backup.ps1` 的 `MEMORY` 模式，更新相关 Rule 与 Skill，并同步修订 HTML 说明页
**产出文件**：`.agent/rules/memory-rules.md`、`.agent/rules/version-control-rules.md`、`.agent/skills/版本控制备份/SKILL.md`、`.agent/skills/版本控制备份/scripts/backup.ps1`、`.agent/skills/记忆管理/SKILL.md`、`.agent/skills/子项目管理/SKILL.md`、`output/03_项目运作机制_202605031830/项目运作机制说明_v1.2.0.html`

---

## 2026-05-03 20:05

**用户问**：纠正 `output` 文件的版本留存规则，要求旧版本带版本号进入 `.history`，工作区只保留最新版本
**AI做了**：移除 `FILE` 模式中“无版本号历史备份”和“重命名失败时复制新文件”的旧逻辑，改为历史文件保留原版本号，并清理 02/03 子项目目录中的旧版本文件
**产出文件**：`.agent/rules/version-control-rules.md`、`.agent/skills/版本控制备份/SKILL.md`、`.agent/skills/版本控制备份/scripts/backup.ps1`、`output/03_项目运作机制_202605031830/项目运作机制说明_v1.2.3.html`

---

## 2026-05-03 21:16

**用户问**：检查当前 Skill 是否会导致 `.memory/知识提炼` 当前区保留版本文件，必要时修复并整理
**AI做了**：修复 `记忆管理` 与 `版本控制备份` Skill 中对 `.memory` 当前态/历史态边界的防线，在备份脚本中拒绝把 `.memory` 下 `*_v*.md` 当 live 文件处理，并清理当前区误留的版本文件
**产出文件**：`.agent/skills/记忆管理/SKILL.md`、`.agent/skills/版本控制备份/SKILL.md`、`.agent/skills/版本控制备份/scripts/backup.ps1`、`.history/.memory/current-zone-cleanup_202605032113/`

---

## 2026-05-03 23:03

**用户问**：设计并构建一个"从0创建"的知识研究工作区初始化工具，要求输出为单文件 EXE，放入目标目录双击即可初始化
**AI做了**：
1. 确认技术路线：Python + PyInstaller，tkinter 弹窗，模板包方式
2. 确认并安装 PyInstaller 6.20.0
3. 在 `05_工作区初始化工具_202605032303` 子项目中构建完整工具包
4. 将 4 个 Rules + 5 个 Skills（完整子目录）复制到 `src/templates/`
5. 编写 `初始化工作区.py` 主脚本（自动感知 EXE 所在目录、创建骨架、弹窗提示）
6. 编写 `build.ps1` 一键编译脚本（已修复中文路径兼容性问题）
7. 成功编译为 `dist/初始化工作区.exe`（10.9 MB）

**产出文件**：
- `output/05_工作区初始化工具_202605032303/src/初始化工作区.py`
- `output/05_工作区初始化工具_202605032303/src/build.ps1`
- `output/05_工作区初始化工具_202605032303/src/templates/`（rules + 5 skills 完整文件）
- `output/05_工作区初始化工具_202605032303/dist/初始化工作区.exe`

**注意事项**：
- PyInstaller 不支持中文路径作为 --add-data 相对路径，必须使用绝对路径
- 编译入口文件须用 ASCII 文件名（init_workspace.py），EXE 输出名可中文（--name 参数）
- build.ps1 已自动处理上述兼容性问题

---

## 2026-05-03 23:54

**用户操作**：将项目中 `.agent` 目录统一改名为 `.agents`，需要同步修正工作区初始化工具的源码、模板和 EXE
**AI做了**：
1. 在 `05_工作区初始化工具_202605032303`（05 v1.1.0）中全面替换 `.agent` → `.agents`
   - `初始化工作区.py` / `init_workspace.py`：文档注释、skeleton_dirs 列表、rules/skills 复制目标路径、弹窗文本、安全检查（8 处）
   - `templates/skills/版本控制备份/scripts/backup.ps1`：注释与 AUTO 模式正则匹配
   - `templates/skills/版本控制备份/SKILL.md`：表格、示例命令、输出示例、目录结构图、命名规则、禁止行为（约 20 处）
   - `templates/rules/version-control-rules.md`：2 处
   - `templates/skills/子项目管理/SKILL.md`：2 处
2. 验证无残留 `.agent` 引用（grep 确认）
3. 重新编译 `dist/初始化工作区.exe`（此时无版本号）

**影响范围**：全局，`.agent` 已统一更名为 `.agents`，初始化工具中的模板已同步。

---

## 2026-05-04 01:30

**用户操作**：需要更新"初始化工作区.exe"，发现 05/06 两个子项目实际上是在同一个工具上迭代，要求合并并加版本号
**AI做了**：
1. 归档 05 v1.0.0 → `.history/output/05_工作区初始化工具_202605032303/`
2. 归档 06 内容 → `.history/output/05_工作区初始化工具_202605032354/`（作为 05 v1.1.0 快照）
3. 用当前项目最新 `.agents/` 刷新 05 的 `src/templates/`（含 memory-rules 加固、记忆管理检查清单、知识提炼触发规则）
4. 创建 `版本记录.md`（含 v1.0.0/v1.1.0/v1.2.0 三版变更记录）
5. `build.ps1` 增加 `$Version` 变量，EXE 输出改为 `初始化工作区_v1.2.0.exe`
6. 编译 `dist/初始化工作区_v1.2.0.exe`（11.5 MB）
7. 删除 `06_Agent目录重命名_202605032354/` 子项目及对应对话记录
8. 清理全局知识地图、系统记录中 06 相关引用
**影响范围**：`output/05_工作区初始化工具_202605032303/` 更新为 v1.2.0，06 已清理。

---

## 2026-05-04 02:35

**用户问**：帮我落地子项目备份方案重新设计
**AI做了**：
1. 设计阶段：经过多轮讨论确认最终方案
   - output/ 子项目文件夹命名从 `编号_主题_时间戳` 改为 `编号_主题_v{x.y.z}`
   - 备份方式从逐文件 FILE 模式统一为整文件夹 PROJECT 模式
   - 备份流程：Copy 整文件夹 → .history（加时间戳后缀）→ Rename live 文件夹 bump 版本号
   - 每个子项目含 `版本记录.md`，记录整体版本历史和子文件变更明细
   - 子项目版本号与子文件版本号互相独立
2. 改造 `backup.ps1`：
   - 新增 PROJECT 模式（AUTO 检测：output/ 下文件夹自动路由）
   - 移除 output/ 的 FILE 模式路由（output/ 单文件会报错提示用 PROJECT）
   - 新增 `Get-ProjectFullName`、`Get-ProjectTopic`、`Update-VersionRecord` 函数
   - `版本记录.md` 格式同 05 现有格式：`## v{x.y.z} (yyyy-mm-dd hh:mm:ss)`
   - 修复 `Set-Content` 中文路径编码问题，改用 `[System.IO.File]::WriteAllText()`
   - `.history/` 快照命名：`{编号}_{主题}_v{x.y.z}_{yyyyMMddHHmmss}`
   - 时间戳精度提升到秒级
3. 改造 `new_project.ps1`：
   - 文件夹命名从时间戳改为 `v1.0.0`
   - 自动创建 `版本记录.md`（含 v1.0.0 MAJOR 初始条目）
   - 移除旧的 版本管理.md 复制逻辑
4. 更新 `版本控制备份/SKILL.md`、`子项目管理/SKILL.md`、`version-control-rules.md`、`filename-rules.md`
5. 迁移 5 个现有子项目（重命名 + 创建版本记录.md）
6. 测试：对 05 执行 PROJECT 备份，成功将 v1.2.0 → v1.2.1
7. 同步更新 `.memory/对话记录/` 5 个文件名匹配新命名
**产出文件**：
- `.agents/skills/版本控制备份/scripts/backup.ps1`（全面重构）
- `.agents/skills/子项目管理/scripts/new_project.ps1`（重构）
- `.agents/skills/版本控制备份/SKILL.md`
- `.agents/skills/子项目管理/SKILL.md`
- `.agents/rules/version-control-rules.md`
- `.agents/rules/filename-rules.md`
- `output/01_版本管理_v1.0.1/版本记录.md`
- `output/02_记忆系统治理方案_v1.1.1/版本记录.md`
- `output/03_项目运作机制_v1.2.3/版本记录.md`
- `output/04_项目骨架初始化_v1.0.0/版本记录.md`
- `output/05_工作区初始化工具_v1.2.1/版本记录.md`
**影响范围**：全局 backup 体系重构，output/ 命名规则统一，.history 历史快照结构更新。

---

## 2026-05-04 02:58

**用户问**：乱码脚本是怎么产生的？
**AI做了**：分析根因——`backup.ps1` 保存为 UTF-8 without BOM，PowerShell 按 GBK 解析中文硬编码字符串（如 `"版本记录.md"`），导致 `Update-VersionRecord` 写出乱码文件 `鐗堟湰璁板綍.md`。修复方案：将脚本转为 UTF-8 BOM。
**影响范围**：`backup.ps1` BOM 修复，后续版本记录写入正常

---

## 2026-05-04 03:10

**用户问**：需要，结束之后再更新 exe
**AI做了**：
1. FOLDER 备份记忆管理 Skill
2. 更新记忆管理 SKILL.md：新增"核心触发规则"章节（4 条必须记录 / 3 条无需记录），执行检查清单第 0 项改为"本轮是否产生信息增量"，第 5 项改为"先写入记忆再发送回复"
3. 更新记忆管理 description
4. CONFIG 备份 memory-rules.md 并更新：强制条款新增信息增量说明，路由后新增执行顺序规则
5. PROJECT 备份 05 v1.3.2 → v1.4.0
6. 同步 templates → 编译 `初始化工作区_v1.4.0.exe`（11 MB）
**产出文件**：
- `.agents/skills/记忆管理/SKILL.md`（核心触发规则 + 检查清单加固）
- `.agents/rules/memory-rules.md`（信息增量 + TodoWrite todo 规则）
- `output/05_工作区初始化工具_v1.4.0/`
- `dist/初始化工作区_v1.4.0.exe`
**影响范围**：记忆管理规则固化，EXE 更新到 v1.4.0

---

## 2026-05-04 03:33

**用户问**：agents 又更新了，重新更新 exe
**AI做了**：
1. PROJECT 备份 05 v1.4.1 → v1.4.2（先备份再操作）
2. 同步 templates → 编译 `初始化工作区_v1.4.2.exe`（11 MB）
**产出文件**：`output/05_工作区初始化工具_v1.4.2/`、`dist/初始化工作区_v1.4.2.exe`

---

## 2026-05-04 12:55

**用户问**：exe 封装了几个 skill？指出 artifact-driven-architect 和 er-diagram-generator 不是正式 Skill
**AI做了**：从模板中移除两个英文 Skill，修正 init_workspace.py 注释 7 → 5，重编译 `初始化工作区_v1.4.3.exe`（10.9 MB）
**产出文件**：`output/05_工作区初始化工具_v1.4.3/`、`dist/初始化工作区_v1.4.3.exe`

---

## 2026-05-05 01:28

**用户问**：检查当前 EXE 是否能在老项目更新骨架，若不行就调整。
**AI做了**：
1. 确认旧 `初始化工作区_v1.4.3.exe` 只能覆盖式复制旧模板：不先备份受管 Rules/Skills，不替换旧 Skill 目录残留，也缺少 `项目规范化`。
2. 升级 `output/05_工作区初始化工具_v1.6.0/src/init_workspace.py`：老工作区运行时先备份受管 rules/skills 到 `.history/.agents/`，再替换内置模板；memory/input/output 只补缺。
3. 全量同步当前 `.agents` 到 `05/src/templates`，更新 `build.ps1` 到 v1.6.0，并加入清理旧 build cache/旧 EXE 后再构建。
4. 重新构建 `dist/初始化工作区_v1.6.0.exe`，并用临时老项目验证“注入骨架 + 规范化迁移”最终 `--Check` 为 0 FAIL / 0 WARN。

**产出文件**：`output/05_工作区初始化工具_v1.6.0/`、`dist/初始化工作区_v1.6.0.exe`

---

## 2026-05-05 11:04

**用户问**：继续三分类改造，补完 `normalize.ps1` 与 `framework-check.ps1`。
**AI做了**：
1. `normalize.ps1` 新增三分类子文件夹识别、空项目补齐、分类版本校正、三节 `目录.md` 生成；仍是平铺内容的老项目不自动搬迁。
2. `framework-check.ps1` 新增只读检查项：新结构/空项目三分类完整性、分类版本与内容状态、内部文件 `{NN}_` 编号和 `_v*` 后缀残留。
3. 修正 `new_project.ps1` 版本记录表头多余竖线，更新 `next_number.ps1` 说明为内部文件无版本号。
4. 已验证四个 `.ps1` 均可解析且保留 UTF-8 BOM；隔离测试确认 `new_project.ps1` 会创建三分类子文件夹，`next_number.ps1 -SubFolder "02"` 返回 `01`。

**验证结果**：`framework-check.ps1` 与 `normalize.ps1 -Check` 可运行；当前红项为既有老项目缺 `目录.md`、未编号文件，以及空项目 `07_运动打嗝原因_v1.0.0` 待补三分类。

**产出文件**：`.agents/skills/项目规范化/scripts/normalize.ps1`、`.agents/skills/框架体检/scripts/framework-check.ps1`、`.agents/skills/子项目管理/scripts/new_project.ps1`、`.agents/skills/子项目管理/scripts/next_number.ps1`

---

## 2026-05-05 19:34

**用户问**：评估 Skills/Rules/exe 是否匹配新标准并适配。
**AI做了**：(1) 修复 remove.ps1 BOM 丢失（Python write_bytes + UTF-8 BOM）。(2) 修正 workspace-spec.json coreSubproject.name 字段 bug：去掉 00_ 前缀，因为 check 脚本会自动加前缀，原值导致 pattern = ^00_00_系统治理_v*$。(3) 重写 init_workspace.py（v1.7.2->v1.8.0）：新增 6 骨架目录 + _init_system_standards() + _init_core_subproject()。
**产出文件**：remove.ps1（BOM 修复）、workspace-spec.json（bug 修复）、init_workspace.py（v1.8.0）

---

## 2026-05-05 19:57

**用户问**：Skills 脚本产出是否都与标准匹配，脚本来自哪个 Skill？
**AI做了**：梳理 7 个 ps1 脚本的 Skill 归属；发现 normalize.ps1（项目规范化 Skill）有 3 处产出不匹配：(1) .memory/ allowedDirs 缺 .system，(2) .history/ allowedHist 缺 .system，(3) 无核心子项目 00_系统治理 检查逻辑。FOLDER 备份项目规范化 Skill 后修复全部三处，BOM 通过 Python 补写。
**验证**：normalize --Check 0 FAIL，3c [PASS]，.memory/.system 不再误报。
**产出文件**：.agents/skills/项目规范化/scripts/normalize.ps1（MINOR 更新）

---

## 2026-05-06 23:15 · backup.ps1 相对路径与镜像落点防线

**用户问**：项目路径会频繁更换，是否可以获取当前项目路径再拼接相对路径，并增加防线防止 `.agents/skills` 备份落到 `.history/skills`。
**AI做了**：修复 `backup.ps1` 的路径入口：从脚本所在位置动态定位当前项目根目录，将 `-TargetPath` 解析为项目内路径并校验不能越出项目；FOLDER 模式只允许单个 `.agents/skills/<Skill名>` 或 `.system/<子文件夹>`，并阻断 `.history/skills/` 错位落点。
**验证**：PowerShell 解析通过；相对路径 `.agents\skills\课题研究` 成功备份到 `.history\.agents\skills\课题研究_20260506231444`；相对路径 `.agents\rules\version-control-rules.md` 成功备份；`.agents\skills` 总目录被拒绝。
**产出文件**：`.agents/skills/版本控制备份/scripts/backup.ps1`

---

## 2026-05-05 21:55

**用户问**：现在的项目规范化脚本做了哪些事情？
**AI做了**：按当前 `normalize.ps1` 梳理其七段检查与 `--Fix` 行为：根目录、`.agents`、`output` 子项目、核心子项目、`.memory`、`.history`、`.system/standards`、`.temp/input`，并明确平铺老项目当前不会无条件迁移为三目录结构。

---

## 2026-05-05 22:25

**用户问**：用新版 `项目规范化` Skill 试着将 00 规范化。
**AI做了**：试运行双阶段规范化：脚本检查先给出 0 FAIL / 9 WARN；大模型自查识别旧 `00_系统治理_v1.0.0` 与当前 live 00 重复、全局知识地图错位、旧对话记录残留；备份后归档旧 00 与旧对话记录，并将地图收敛到 `00_系统治理_v1.3.1`。最终复查为 0 FAIL / 7 WARN，00 相关问题已消除，剩余 WARN 属于 09 子项目和旧历史快照。
**产出文件**：`output/00_系统治理_v1.3.1/`、`.memory/全局知识地图.md`

---

## 2026-05-05 22:51

**用户问**：同步修改剩余的 Skills 和 Rules。
**AI做了**：同步脚本检查口径：`normalize.ps1` 与 `framework-check.ps1` 不再把 01/02 内带 `_v*` 文件版本当作异常，改为检查 01/02 内容文件是否缺少文件版本；03 不强制文件级版本。两脚本 PowerShell 解析通过，UTF-8 BOM 保持正常。
**验证**：`normalize.ps1 --Check` 为 0 FAIL / 11 WARN；`framework-check.ps1` 为 0 FAIL / 1 WARN，WARN 现指向 16 个既有 01/02 内容文件缺少文件版本。
**产出文件**：`.agents/skills/项目规范化/scripts/normalize.ps1`、`.agents/skills/框架体检/scripts/framework-check.ps1`

---

## 2026-05-05 23:18

**用户问**：重新规范化 00 子项目。
**AI做了**：执行 00 专项规范化后发现检查脚本仍将有内容的 `01/02` 文件夹固定期望为 `v1.0.0`，与“内容集合变化同步 bump”新标准冲突；已修正 `normalize.ps1` 与 `framework-check.ps1` 的期望逻辑，允许有内容的 01/02 文件夹保持 `v1.1.0+`。两脚本解析和 BOM 均正常。
**验证**：`normalize.ps1 --Check` 为 0 FAIL / 10 WARN；`framework-check.ps1` 为 0 FAIL / 1 WARN，00 已 PASS，剩余 WARN 均非 00 范围。
**产出文件**：`.agents/skills/项目规范化/scripts/normalize.ps1`、`.agents/skills/框架体检/scripts/framework-check.ps1`

---

## 2026-05-06 00:42

**操作**：全量同步模板并重新编译 `初始化工作区_v2.0.0.exe`。
**AI做了**：(1) 用临时 Python 脚本 `.temp/sync_templates.py` 做 MD5 对比同步，更新 filename-rules.md、8个 Skill、3个 .system/standards 标准文件。(2) 通过 `.temp/update_build_ps1.py` 以 utf-8-sig 方式更新 build.ps1 到 v2.0.0（保持 BOM）。(3) PyInstaller 6.20.0 编译 11 MB EXE。
**注意**：子文件夹 v1.11.0→v1.12.0 重命名因 IDE 句柄锁定失败，备份快照已生成，待用户在文件管理器手动完成重命名。

---
## 2026-05-06 03:13

**用户问**：更新 05 子项目并封装 EXE。
**AI做了**：将初始化工具源码升级到 `SKELETON_VERSION=2.2.0`、`SSO_SPEC_VERSION=1.3.0`，删除初始化骨架中的 `.memory/.system` 创建项，同步内置 standards 与 `项目规范化` 模板；源码初始化验证通过，PyInstaller 重新封装 `初始化工作区_v2.2.0.exe`。
**验证**：`py_compile` 通过；源码测试目录未生成 `.memory/.system`；`normalize.ps1 --Check` 为 0 FAIL；`framework-check.ps1` 为 0 FAIL / 1 个既有命名 WARN。
**产出文件**：`output/05_工作区初始化工具_v1.15.0/03_代码程序_v0.0.0/src/init_workspace.py`、`output/05_工作区初始化工具_v1.15.0/03_代码程序_v0.0.0/src/build.ps1`、`output/05_工作区初始化工具_v1.15.0/03_代码程序_v0.0.0/dist/初始化工作区_v2.2.0.exe`

---

---

## 2026-05-06 03:33 · 03_代码程序目录不能只按直接文件判空

**问题**：旧检查脚本调用 Get-SubFolderContentFiles，只统计直接文件； 3_代码程序 的真实内容通常是 src/、dist/、uild_tmp/ 等目录，因此会被误判为空目录并期望 0.0.0。

**处理**：对  3_代码程序 改为统计有效内容项；普通  1/02 分类目录仍保留直接文件判定。修复后当前工作区  3_代码程序_v2.2.1 通过规范化与体检。

---

## 2026-05-07 00:08

**用户问**：更新 05 子项目并重新封装 EXE。
**AI做了**：按 PROJECT 备份升级 05 子项目到 `v1.16.0`，将代码程序目录升至 `03_代码程序_v2.3.0`，把 `SKELETON_VERSION` 与 `build.ps1` 输出版本升至 `2.3.0`，用 PyInstaller 6.20.0 封装 `初始化工作区_v2.3.0.exe`；源码烟测生成 4 rules / 8 skills / 3 standards 且 errors=0。
**产出文件**：`output/05_工作区初始化工具_v1.16.0/03_代码程序_v2.3.0/dist/初始化工作区_v2.3.0.exe`

---
