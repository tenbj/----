# 系统记录 · Rule 变更

> Rule 文件的增删改历史，按时间追加。

---

## 2026-05-06 23:15 · version-control-rules 增加备份路径防线

**用户问**：防止 AI 绕过脚本把 Skill 备份放到错误的 `.history/skills`。
**AI做了**：在 always-on 版本控制规则中增加强制约束：备份必须由 `.agents/skills/版本控制备份/scripts/backup.ps1` 生成，禁止手工拼接 `.history`；项目路径可变时优先使用项目内相对路径；`.agents/skills` 的唯一合法历史落点为 `.history/.agents/skills/{Skill名}_{yyyyMMddHHmmss}/`。
**产出文件**：`.agents/rules/version-control-rules.md`

---

## 2026-05-04 21:30

**用户问**：子项目内文件多了一团乱，如何管理
**AI做了**：讨论并确定了"文件编号 + 目录.md"方案，更新了三个 Rule 文件：
- `filename-rules.md`：新增第6条（内容文件命名：{NN}_{主题}_v{x.y.z}.ext）和第7条（固定文件不带编号）
- `direction-rules.md`：新增第5条（目录.md 维护规则：新建/MAJOR/MINOR 时更新，PATCH 不触发）
- `version-control-rules.md`：补 目录.md 随子项目整体快照备份的说明，并在 .ps1 影响范围中增加 next_number.ps1 和 normalize_project.ps1
**产出文件**：`filename-rules.md`、`direction-rules.md`、`version-control-rules.md`

---

## 2026-05-03 15:31

**用户问**：以 `.history` 为准
**AI做了**：更新 `version-control-rules.md` 第 6 条，统一备份路径为 `.history`

---

## 2026-05-03 15:38

**用户问**：对三个 Rule 文件全部过一遍
**AI做了**：更新三个 Rule 文件，修正版本格式（两段变三段），添加 Skill 路由，精简执行步骤
**产出文件**：`direction-rules.md`、`filename-rules.md`、`version-control-rules.md`

---

## 2026-05-03 15:50

**用户问**：设计对话记忆留存系统
**AI做了**：设计并确认三种记忆类型及触发机制，形成初版记忆系统思路

---

## 2026-05-03 16:51

**用户问**：确认记忆系统方案，开始实施
**AI做了**：创建 `记忆管理` Skill、`memory-rules.md`，更新 `子项目管理` Skill
**产出文件**：`.agent/skills/记忆管理/SKILL.md`、`.agent/rules/memory-rules.md`

---

## 2026-05-03 18:06

**用户问**：看不明白方案，要求先直接修改现有规则与记忆结构
**AI做了**：将对话记忆改为 `PROJECT / SYSTEM / BOTH` 路由，新增 `.memory/系统记录/rules-skills.md`，并把已有系统治理记录从子项目对话记录中迁移出来
**产出文件**：`.agent/rules/memory-rules.md`、`.agent/skills/记忆管理/SKILL.md`、`.agent/skills/子项目管理/SKILL.md`、`.memory/系统记录/rules-skills.md`

---

## 2026-05-04 00:00（第四次）

**用户问**：告诉我怎么解决知识提炼没触发的问题
**AI做了**：
1. 备份 `记忆管理 SKILL.md` → `.history/.agents/skills/记忆管理_202605040001/SKILL.md`
2. 统一知识提炼触发规则，消除"必须"与"手动触发"的矛盾
   - `记忆管理 Skill`：触发时机从"手动触发"改为"每次 output 文件增删改后自动触发"；新增三类触发条件（output 实质性变更 / 系统治理可复用认知 / 用户要求）
   - `memory-rules.md`：条款收紧为"根据触发条件判断，PATCH 不触发，MAJOR/MINOR 或系统治理认知必须触发"
**影响范围**：`.agents/skills/记忆管理/SKILL.md`、`.agents/rules/memory-rules.md`

---

## 2026-05-04 14:00

**用户问**：排查"新话题未建子项目"的框架根因，并落地修复
**AI做了**：
1. 排查根因：三道断点——direction-rules 缺少话题归属步骤、子项目管理 Skill 触发条件与"输出文件"耦合、"纯聊天"模式绕过记忆记录
2. 设计修复方案：两道独立强制门（话题归属门 + 知识回答记忆记录门）
3. 执行 4 份备份（direction-rules.md CONFIG、子项目管理 FOLDER、memory-rules.md CONFIG、记忆管理 FOLDER）
4. 修改 4 个文件落地修复：
   - `direction-rules.md`：顶部新增"话题归属强制门"，要求每次知识提问前先扫描 output/ 判定归属
   - `子项目管理/SKILL.md`：触发条件从"需要研究+输出文件"改为"新知识话题即触发"
   - `memory-rules.md`：新增"知识回答硬触发"，独立于信息增量判断、优先级最高
   - `记忆管理/SKILL.md`：执行检查清单第 0 项改为"本轮是否输出了知识性回答"，必须记录条款第 1 条补"包括聊天中的知识回答"
**产出文件**：
- `.agents/rules/direction-rules.md`（新增话题归属强制门）
- `.agents/skills/子项目管理/SKILL.md`（解除输出文件耦合）
- `.agents/rules/memory-rules.md`（新增知识回答硬触发）
- `.agents/skills/记忆管理/SKILL.md`（强化第 0 项+触发规则）
**根因**：框架用"需要研究+输出文件"作子项目触发条件，AI 把纯聊天知识回答判定为不触发，导致子项目漏建+记忆漏记。
**修复逻辑**：两道独立门——话题归属门（回答前强制扫描 output/）堵死纯聊天豁免；记忆记录门（知识回答硬触发）堵死没调 Skill 就不写记忆。

---

## 2026-05-05 11:04

**用户问**：继续子项目内部三级分类改造。
**AI做了**：更新 `filename-rules.md`，把子文件版本号规则改为三分类子文件夹承载版本号：`01_问题答疑_v*`、`02_课题研究_v*`、`03_代码程序_v*`；内部文件改为 `{NN}_{标题}.{扩展名}`，不再带 `_v*`。
**产出文件**：`.agents/rules/filename-rules.md`

---

## 2026-05-05 17:19

**用户问**：新增 `.system` 后，需要从 Skill 中提炼各子项目命名规范初版。
**AI做了**：读取 `子项目管理`、`项目规范化`、`filename-rules.md`、`workspace-spec.json` 与 `09_项目检查标准`，提炼并落盘人类可读的子项目命名规范；明确新标准以 Skill + `.system` JSON 为准，旧内部文件 `_v*` 属于待迁移遗留。
**产出文件**：`.system/standards/子项目命名规范.md`

---

## 2026-05-05 17:23

**用户问**：命名规范不只需要子项目，还需要覆盖 `.history` 和 `.memory`。
**AI做了**：新增工作区级命名规范，统一说明 `output/` 当前产出、`.memory/` 当前态记忆、`.history/` 历史快照之间的命名归属；补齐对话记录、系统记录、知识提炼、全局知识地图、output 快照、.agents 备份、.memory 历史版本的命名规则。
**产出文件**：`.system/standards/工作区命名规范.md`

---

## 2026-05-05 19:13

**用户问**：子项目命名规范已合并进工作区命名规范，为什么还保留独立文件。
**AI做了**：纠正 `.system` 标准骨架，删除独立 `子项目命名规范.md`，并从 `workspace-spec.json` 与 `工作区骨架规格.md` 中移除该核心文件项；现在工作区命名规范是唯一命名标准入口。
**产出文件**：`.system/standards/workspace-spec.json`、`.system/standards/工作区骨架规格.md`

---

## 2026-05-05 19:21

**用户问**：仍能看到 `子项目命名规范.md`，质疑工作区命名规范是否已收敛。
**AI做了**：复查当前态 `.system/standards`，确认 live 标准目录只有 `workspace-spec.json`、`工作区骨架规格.md`、`工作区命名规范.md`；清理 `工作区命名规范.md` 内残留的目录示例引用，明确人读命名标准只保留工作区命名规范。
**产出文件**：`.system/standards/工作区命名规范.md`

---
## 2026-05-05 22:39

**用户问**：纠正子项目内部版本规则，强调 01/02 文件需要文件级版本，03 才作为整体使用文件夹版本，并要求优先调整标准。
**AI做了**：优先修改 `.system/standards/` 三份标准：`workspace-spec.json`、`工作区骨架规格.md`、`工作区命名规范.md`。新口径为：`01_问题答疑` 与 `02_课题研究` 内内容文件必须带独立 `_v{x.y.z}` 文件版本；`03_代码程序` 由 `03_代码程序_v{x.y.z}` 文件夹版本表示整体代码程序版本，内部文件不强制文件级版本。脚本、Skill、00 文件名恢复尚未展开。
**产出文件**：`.system/standards/workspace-spec.json`、`.system/standards/工作区骨架规格.md`、`.system/standards/工作区命名规范.md`

---

## 2026-05-05 22:41

**用户问**：补充 01/02 文件夹版本也要随内容新增删除或内容文件版本修改一同修改。
**AI做了**：继续优先调整 `.system/standards/`，明确 `01_问题答疑_v*` 与 `02_课题研究_v*` 的文件夹版本表示该分类下内容文件集合版本；新增内容文件、删除内容文件、任一内容文件版本升级，都必须同步升级对应分类文件夹版本。`03_代码程序_v*` 仍表示代码程序整体版本。
**产出文件**：`.system/standards/workspace-spec.json`、`.system/standards/工作区骨架规格.md`、`.system/standards/工作区命名规范.md`

---

## 2026-05-05 22:51

**用户问**：同步修改剩余的 Skills 和 Rules。
**AI做了**：更新 `filename-rules.md`，将子项目内部命名规则同步为：`01/02` 内容文件必须带独立文件版本，`01/02` 分类文件夹版本随内容集合变化同步 bump，`03` 作为代码程序整体由文件夹版本管理。
**产出文件**：`.agents/rules/filename-rules.md`

---
## 2026-05-06 03:13

**用户问**：认为 `.memory/.system/` 路径多余，要求从标准中剔除并检查 rules/skills。
**AI做了**：更新 `.system/standards/workspace-spec.json` 至 v1.3.0，移除 `.memory/.system` 和 `.memory/.system/standards` 必备目录、`systemCore.memoryMirrorDirectories` 以及 `.memory` 允许目录中的 `.system`；同步修订两份人类可读标准。Rules 中未发现需修改的直接引用。
**产出文件**：`.system/standards/workspace-spec.json`、`.system/standards/工作区骨架规格.md`、`.system/standards/工作区命名规范.md`

---

## 2026-05-07 23:41 · 三道防线 Rule 第一轮落地

**用户问**：授权按待修改清单第一轮执行，修改 `.agents/rules`。
**AI做了**：修改 `direction-rules.md`、`version-control-rules.md`、`memory-rules.md`：新增对话路由门、核心骨架授权门、`.system`/版本化 `.memory` 备份口径和对话路由到记忆的固定映射。
**产出文件**：`.agents/rules/direction-rules.md`、`.agents/rules/version-control-rules.md`、`.agents/rules/memory-rules.md`

---