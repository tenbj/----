# 标准点 #6 · .temp 与 input 检查

> 检查：`.temp/` 和 `input/` 是否存在。修正：缺少则创建。

---

## 标准

| 文件夹 | 用途 |
|--------|------|
| `.temp/` | 临时文件、规范化异常归档 |
| `input/` | 参考资料（AI 不输出到此处） |

两个文件夹**只需要存在**，不检查内部结构。

---

## 检查

- `.temp/` 是否存在
- `input/` 是否存在

## 修正

- 缺少 → 创建空文件夹
